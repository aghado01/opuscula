# Persistent (Co)Homology in Matrix Multiplication Time

Dmitriy Morozov 1 and Primoz Skraba 2

## Contents

  - [Abstract](#abstract)
- [1 Introduction](#1-introduction)
- [2 Preliminaries](#2-preliminaries)
  - [2.1 Persistence Algorithm](#21-persistence-algorithm)
  - [2.2 Cycle Representatives](#22-cycle-representatives)
- [3 Matrix Preliminaries](#3-matrix-preliminaries)
- [4 Column Algorithms](#4-column-algorithms)
  - [4.1 Fast Exhaustive Algorithm](#41-fast-exhaustive-algorithm)
- [5 Row Algorithm](#5-row-algorithm)
  - [5.1 Fast Row Algorithm](#51-fast-row-algorithm)
- [6 Discussion](#6-discussion)
- [Acknowledgement](#acknowledgement)
- [A Matrix Inversion in Matrix Multiplication Time](#a-matrix-inversion-in-matrix-multiplication-time)
- [B Analysis of Persistence Recursion](#b-analysis-of-persistence-recursion)
- [References](references/2412.02591v2.md)

## 1 Introduction

Persistent homology [ 2 ] is one of the core techniques in topological data analysis. Because of its theoretical importance as well as its wide use in applications, a lot of work has been dedicated to its efficient computation both in theory [ 1 , 3 , 4 , 5 , 6 ] and in practice [ 7 , 8 , 9 , 10 , 11 , 12 ]. On the theoretical side, the connection between persistent homology and Gaussian elimination was established early on [ 13 ], and as a result it has been long accepted that persistence can be computed in matrix multiplication time. Milosavljevic et al. [ 1 ] gave an explicit algorithm for the more general case of zigzag persistence [ 14 , 15 ]. The main difficulty in applying standard linear algebra techniques is the strict constraint on the row and column ordering of the boundary matrix.

As persistent homology evolved as a field, the significance of its formulation as an $R =$ DV decomposition [ 16 ] of the input boundary matrix D has become clear. The cycles and chains recovered from this decomposition found many uses in attributing topological features to the input data [ 17 , 18 , 19 ]. However, it is not immediately clear from the algorithm in [ 1 ] how to recover the matrices R and V necessary in applications.

Instead of trying to deconstruct the algorithm of Milosavljevic et al. [ 1 ], we simplify it for the case of ordinary persistence. We describe how to compute two different forms of the $R =$ DV decomposition that come up in applications lazy and exhaustive reductions which lets us recover specific cycle representatives in matrix multiplication time. As a benefit, because of its simpler setting, the algorithms in this paper are greatly simplified, compared to [ 1 ]. Additionally, using Dey and Hou’s fast zigzag construction [ 20 ], which reduces computation of zigzag persistence to ordinary persistence; the algorithms in this paper give another way to compute zigzag persistence in matrix multiplication time.

## 2 Preliminaries

We are concerned with the persistent homology of a filtration, an increasing sequence of simplicial complexes indexed by the natural numbers N :

$$
\emptyset = K_{0} \subset K_{1} \subset \dots \subset K_{N} = K
$$

While persistent homology can be defined more generally, from an algorithmic perspective this is sufficiently general as nearly all considered cases may be reduced to this setting. We further assume that $| K i - K i - 1 | = 1$ , or equivalently that the filtration is a total order, i.e., each step in the filtration adds a single cell. If the initial filtration does not satisfy this requirement, and defines only a partial order, we may always extend it to an arbitrary compatible total order, e.g., breaking ties by lexicographical ordering and dimension.

Notation Throughout, we use the following notation:



• Matrices are denoted by bold capital letters, e.g., M , with sub-matrices indexed by square brackets, e.g., the j -th column is M [ · ,j ], the i -th row is M [ i, · ].

• Indexing starts at 1 and indices may be indexed by sets, e.g., A = { 1 ,...,k } , M [ · ,A ] refers to the first k columns. Note that the set may not refer to contiguous columns/rows.

• We consider permutations implicitly, so ( P · M )[ A, · ] refers to the rows indexed after the permutation P is applied to matrix M . For all the algorithms, performing an explicit permutation, then undoing it, does not affect the asymptotic running time.

### 2.1 Persistence Algorithm

For completeness, we recount the standard persistence algorithm, see [ 2 ], [ 13 ], and [ 21 ] for a more complete description. Let D denote the boundary operator, which we represent as an n $×$ n -matrix over some fixed (finite) field F . The rows (top-down) and columns (left-right) are ordered by appearance in the filtration. Hence, the i -th row and column represents the simplex $\sigma = K i - K i - 1$ and D [ $·$ ,i ] represents the boundary of $\sigma$ respectively. The goal is to compute the RV -decomposition [ 16 ]. That is, find matrices R and V such that

$$
R = D \cdot V ,
$$

where R is a column-reduced matrix and V is full-rank upper-triangular; we let $U = V - 1$ . We introduce the helper function

$$
\text{low} ( j ) = \arg \max_{i} R [ i , j ] \neq 0 ,
$$

̸ which is defined whenever $R [ \cdot ,j ] ̸ = 0$ . When defined, it returns the largest row index i where the column is non-zero; we refer to these lowest non-zero elements as pivots . Throughout this paper, low always applies to R . The persistence diagram is then given by:̸

$$
\text{dgm} = \{( i , j ) \ | \ i = \text{low} ( j ) \} \ \cup \ \{( i , \infty ) \ | \ \text{R} [ \cdot , i ] = 0 \text{and} i \neq \text{ow} ( j ) \ \forall j \} \, .
$$

̸ The standard algorithm is given by a variant of Gaussian elimination. Algorithm 1 , which we call the lazy reduction , reduces each column by considering pivots in the columns to the left. In each step of the loop, it removes the lowest non-zero entry until a new pivot is found or the entire column is zeroed out. A straightforward analysis gives a running time bound of O ( n 3 ). In [ 22 ], it was shown that this bound is tight by giving an example filtration where this algorithm takes cubic time. The updates of matrix V follow those of matrix R . The updates in matrix U undo the updates in V to maintain $U \cdot V = I$ . Because the columns of R are processed from left to right, during the update of matrix U , the row U [ j, $·$ ] has a single (diagonal) element. Therefore, the update in U is equivalent to just setting the entry U [ j $′$ ,j ] to $\alpha$ .

Algorithm 1 Standard Persistence Algorithm (Lazy reduction) 1: $R = D$ 2: $V = I n , U = I n$ 3: for $j = 1$ to n do 4: while $R [ \cdot ,j ] ̸ = 0$ and $\exists j ' < j$ such that low $( j ) =$ low ( j $′$ ) do 5: $i \gets$ low ( j ) 6: $\alpha \gets R [ i,j ] / R [ i,j ' ]$ 7: $R [ \cdot ,j ] \gets R [ \cdot ,j ] - \alpha \cdot R [ \cdot ,j ' ]$ 8: $V [ \cdot ,j ] \gets V [ \cdot ,j ] - \alpha \cdot V [ \cdot ,j ' ]$ 9: $U [ j ' , \cdot ] \gets U [ j ' , \cdot ] + \alpha \cdot U [ j, \cdot ]$ (equivalently, $U [ j ' ,j ] \gets \alpha$ ) Algorithm 2 “Look-ahead” Variant of Standard Persistence (Exhaustive reduction) 1: $R = D$ 2: $V = I n , U = I n$ 3: for $j = 1$ to n do 4: if low ( j ) is defined then 5: $i \gets$ low ( j ) 6: for $j ' > j$ and R [ low ( j ) ,j $′$ ] $̸ = 0$ do 7: $\alpha \gets R [ i,j ' ] / R [ i,j ]$ 8: $R [ \cdot ,j ' ] \gets R [ \cdot ,j ' ] - \alpha \cdot R [ \cdot ,j ]$ 9: $V [ \cdot ,j ' ] \gets V [ \cdot ,j ' ] - \alpha \cdot V [ \cdot ,j ]$ 10: $U [ j, \cdot ] \gets U [ j, \cdot ] + \alpha \cdot U [ j ' , \cdot ]$ ( not the same as $U [ j,j ' ] \gets \alpha$ !)

We introduce another variant of persistence computation used in applications. This one “looks ahead” and eliminates as many elements from the matrix as it can; so we call it the exhaustive reduction . In Algorithm 2 , by the time a column is considered, it is already reduced. It is then applied to all columns to the right, zeroing out the entire row. In this way, when a column is processed, all previous pivots have already been applied. Because column R [ $·$ ,j $′$ ] may have already been used to reduce another column R [ $·$ ,j $′′$ ], when an update is applied to it, the row U [ j $′$ , $·$ ] need not consist of a single diagonal element. Hence, the row update in matrix U is not equivalent to just setting U [ j,j $′$ ] to $\alpha$ , like in the lazy version of the algorithm.

Lazy vs. Exhaustive. The algorithms based on the two reductions are in a sense the extreme cases: lazy reduction reduces columns only when necessary; and the exhaustive reduction reduces them as much as possible. To illustrate the difference, in the exhaustive reduction, the entire row to the right of a pivot will be zeroed out, whereas in the case of the lazy reduction, only those columns will be zeroed out which have a conflicting pivot in the same row. There are, of course, any number of possible other reductions between these but we do not know of any applications that rely on them.

Remark 2.1. As noted in [ 3 , Section 3.4 ], persistent cohomology can be computed by performing the same algorithm on the anti-transpose of D . As such all the algorithms in this paper can compute persistent cohomology.

### 2.2 Cycle Representatives

Given an $R =$ DV decomposition, we distinguish between two types of simplices in the filtration: positive simplices create new homology classes; negative simplices destroy them. From the matrix decomposition, one can recover a set of cycle representatives of persistent homology. There are two sources of this information, although with different content.

1. V matrix: The columns of positive simplices in V correspond to zero columns in the reduced matrix R . These columns in V are cycles, by definition.

2. R matrix: The columns of negative simplices in R are non-zero. They store linear combinations of boundaries, which are cycles (at the time of their birth) that eventually die in the filtration. The cycles one recovers from matrix R differ between the lazy and exhaustive reductions. What we call exhaustive reduction, Cohen-Steiner et al. [ 18 ] called total reduction. They show that these cycles form a lexicographically optimal basis, which in turn can be used to triangulate point cloud data. Nigmetov and Morozov [ 19 ] use the lazy reduction specifically, the columns and rows from matrices V and U to recover what they called “critical sets,” subsets of simplicial complexes affected by optimization with the loss formulated in terms of a persistence diagram.

Minimum Spanning Acycle Basis. The negative simplices in the filtration represent a minimal spanning acycle (MSA) [ 23 , 24 ]. The columns in V obtained from either lazy or exhaustive reduction or any reduction that never adds columns of positive simplices to other columns have the form $\tau \in$ MSA $\lambda \tau \tau + \lambda \sigma \sigma$ . The support of the chain is one (positive) simplex $\sigma$ plus a linear combination of simplices in the minimum spanning acycle.

Lemma 2.2. The representatives from V are the same for lazy and exhaustive reduction. Proof. Which columns of R are non-zero does not depend on the algorithm used; the set of negative simplices only depends on the order of the filtration. We observe that the boundaries of the negative simplices form a basis (since their pivots are in distinct rows). By definition, the boundary of a positive simplex may be expressed as a linear combination of boundaries of negative simplices. Since the boundaries of the negative simplices form a basis, this linear combination is unique.

Death Basis. An alternative is to get cycle representatives directly from the R matrix. Its non-zero columns represent cycles because multiplying $D \cdot R =$ 0. This follows directly since $R = D \cdot V$ , so $D \cdot R = D \cdot D \cdot V = 0$ , since $D \cdot D = 0$ . These columns give cycle representatives for all finite classes, i.e., those homology classes which eventually die.

Remark 2.3. Following [ 3 ], we can use the same approach for cohomology applying the same algorithms to $D \perp$ , the anti-transpose of D to obtain the decomposition $R \perp = D \perp \cdot V \perp$ . The cocycle representatives are then given by columns in $V \perp$ . To see that these are cocycles, we observe that by construction, they map to zero via the coboundary operator $D \perp$ before the corresponding death time, i.e., below the pivot in the corresponding column in $R \perp$ , as we have reversed the indexing with the anti-transpose. Likewise, the cocycle representatives of essential cocycles are given by the columns of $V \perp$ whose: corresponding columns in $R \perp$ are zero and they must not be coboundaries, i.e., the corresponding rows in $R \perp$ must not contain pivots.

## 3 Matrix Preliminaries

We recount a few classical results on matrix multiplication. We assume the matrices are over some field F . Lemma 3.1. Let B be an n $×$ k matrix and C be a k $×$ k matrix. The product B $·$ C can be computed in $O ( nk \omega - 1 )$ -time. Proof. Divide B into $⌈$ n k $⌉$ sub-matrices B i of size k $×$ k :

$$
B \cdot C = \left [ \begin{array}{c} B_{1} \\ \vdots \\ B_{\lceil n / k \rceil} \end{array} \right ] \cdot C = \left [ \begin{array}{c} B_{1} \cdot C \\ B_{2} \cdot C \\ \vdots \\ B_{\lceil n / k \rceil} \cdot C \end{array} \right ]
$$

Each B i $·$ C is a product of two k $×$ k matrices, which by definition takes $O ( k \omega )$ time. There are $⌈$ n k $⌉$ products to compute yielding a running time of O n +1 $k k \omega = O ( nk \omega - 1 ).$

Column Operations via Matrix Multiplication. Here we relate matrix multiplication with the reduction steps used in the persistence algorithms, i.e., representing column operations via matrix multiplication. Assume that we have the following block matrix,

$$
\left [ \ B \ | \ C \ \right ] = \left [ \ b ( 1 ) \ \dots \ \ b ( n ) \ | \ c ( 1 ) \ \dots \ \ c ( m ) \ \right ]
$$

where b ( i ) and c ( j ) are (column) vectors. Reducing c ( j ) which we denote c ′ ( j ), using the vectors b ( i ), is expressed as a linear combination

$$
c^{\prime} ( j ) = c ( j ) + \sum_{i} \lambda_{i} ( j ) b ( i ) ,
$$

where $\lambda i ( j )$ are the coefficients. Reducing only the j -th column of C may be written as

$$
\left [ B \ c^{\prime} ( j ) \right ] = \left [ B \ c ( j ) \right ] \cdot \left [ \begin{matrix} \mathbf I & \lambda ( j ) \\ 0 & \mathbf 1_{j} \end{matrix} \right ] ,
$$

where 1 j is a column vector where the j -th entry is 1 and zero everywhere else. If we consider $\Lambda [ i,j ]$ := $\lambda i ( j )$ , we can rewrite the reduction of C by

$$
[ B \ C^{\prime} ] = [ B \ C ] \cdot \left [_{0}^{I} \ \overset{\Lambda}{I} \right ] ,
$$

or equivalently, the reduced matrix is $C ' = C - B \cdot \Lambda$ . Lemma 3.2. Multiplying a permutation matrix with an n × m matrix, takes O ( nm ) time. Proof. As a permutation matrix rewrites each element once, the total number of elements gives the bound.

## 4 Column Algorithms

Here we describe the algorithm in [ 1 ] for the special case of classical persistent homology. To aid in exposition, we first recall the Schur complement . Given a block matrix

$$
\begin{bmatrix} B_{1} & C_{1} \\ B_{2} & C_{2} \end{bmatrix} ,
$$

assuming $B_{2}$ is non-singular, we can compute an updated matrix where the rows corresponding to $B_{2}$ are zeroed out by the following transformation:

$$
\begin{bmatrix} B_{1} & C_{1} \\ B_{2} & C_{2} \end{bmatrix} \begin{bmatrix} I & - B_{2}^{- 1} C_{2} \\ 0 & I \end{bmatrix} = \begin{bmatrix} B_{1} & C_{1} - B_{1} B_{2}^{- 1} C_{2} \\ B_{2} & 0 \end{bmatrix}
$$

where $C_{1} - B_{1} B - 1 2 C_{2}$ is referred to as the Schur complement. Observation 4.1. The matrix $B - 1 2 C_{2}$ encodes the column operations required to zero out $C_{2}$ , that is, $B - 1 2 C_{2} = \Lambda . A$ key fact we will use in the algorithm is that matrix inversion has the same algorithmic complexity as matrix multiplication. Theorem 4.2. Inverting a (square) non-singular (upper or lower) triangular matrix can be done in matrix multiplication time. Proof. See Appendix A for a simplified proof for the special case of triangular matrices.

Algorithm 3 Column Algorithm( A = [ i,j ]) 1: if $i = j$ then $▷$ leaf 2: if $R [ \cdot ,i ] = 0$ then $▷$ If no pivot exists 3: $ℓ \gets arg min$ ℓ>n $R [ \cdot ,ℓ ] ̸ = 0 ▷$ find the next pivot in $I_{m} + n$ 4: Permute columns i and ℓ 5: $Z \gets Z \cup i$ 6: else 7: $B \gets$ [ $i, ( i + j ) / 2$ ) $▷$ left child 8: $C \gets$ [( $i + j$ ) / 2 ,j ] $▷$ right child 9: Recurse on B 10: Apply updates from columns B to C : 11: $L \gets \langle$ low $( ℓ ) : ℓ \in B \rangle ▷$ Implicit permutation; L preserves order 12: $\Lambda \gets R [ L,B ] - 1 \cdot R [ L,C ] ▷ A - 1 2 B_{2}$ 13: $R [ ¯ L,C ] \gets R [ ¯ L,C ] - R [ ¯ L,B ] \cdot \Lambda ▷$ Apply B to C ; Schur update ( k $×$ k ) $·$ ( k $×$ n ) 14: $R [ L,C ] = 0$ 15: $V [ \cdot ,C ] \gets V [ \cdot ,C ] - V [ \cdot ,B ] \cdot \Lambda ▷$ Matching update in V 16: Recurse on C ̸

### 4.1 Fast Exhaustive Algorithm

We present a fast version of exhaustive reduction in Algorithm 3. The general structure follows Algorithm 2. However, rather than apply the pivots when we find them, we apply them in batches via the Schur complement. We first augment the boundary matrix with an identity matrix, which is of size at least n , for reasons we shall describe below. To simplify notation, we assume n is a power of 2. If it is not, we can set the size of the identity matrix to $m + n$ with $m < n$ such that $m + 2 n = 2 x$ . Therefore, our initial matrices are:

$$
R = \left [ \begin{matrix} D & 0 \\ 0 & I_{m + n} \end{matrix} \right ] , \quad V = I_{m + 2 n} .
$$

Throughout the algorithm, we implicitly perform two types of permutations by taking appropriate subsets of rows and columns. The first are column permutations. To ensure that the appropriate submatrices are invertible, we require that after processing k columns of R , there are k pivots, or, equivalently, that the matrix corresponding to the processed columns is full (column) rank. Because cycles reduce to zero columns, the if-statement in Line 2 permutes a zero column from R with a non-zero column from the identity matrix $I_{m} + n$ , and records the indices of such columns in list Z , to eventually undo the permutation. 1

We must also consider row permutations. To apply Theorem 4.2 , we must find the inverse $R - [ L,B ]$ in Line 12 , which is used in the Schur complement. We construct a sequence $L = \langle$ low $R [ \cdot ,ℓ ] : ℓ \in B \rangle$ of the pivot rows in columns B . Denoting by ¯ L the sequence of rows outside of L , we denote a permutation that places rows L below ¯ L as $P_{L}$ : ¯

$$
\begin{aligned}
( P_{L} \cdot R ) = \begin{bmatrix} R [ \bar{L} , \cdot ] \\ R [ L , \cdot ] \end{bmatrix} . \quad & ( 2 )
\end{aligned}
$$

See Figure 2(b-c). The permutation is explicitly given by,

$$
P_{L} \colon \text{low} ( B ( i ) ) \mapsto m + n - | B | + i \quad \text{for} i = 1 , \dots , | B |
$$

where $B ( i )$ is the i -th entry of the indices in B . Lemma 4.3. After permuting the rows by $P_{L}$ , the bottom-left k × k matrix is lower triangular, where $k = | L |$ . We delay the proof until after the description of the algorithm. We now follow a recursion: we split matrix R into the first half of the columns denoted by B and the second half denoted by C . We proceed to call the algorithm on the first half of the matrix. Once we reach a leaf in the recursion tree (shown in Figure 3 ), the matrix consists of a single column. The key invariant is that whenever we reach a leaf, the corresponding column is reduced with respect to all the columns which come prior to it in the filtration. For the first column, this is tautologically true and we delay the proof for the general case until later.

![The image is a bar graph that shows the results of a survey. The graph has four horizontal bars, each representing a different category. The bars are colored blue, orange, green, and purple. The x-axis is labeled a and the y-axis is labeled b. The bars are arranged in a vertical sequence from left to right, with each bar corresponding to a different category. ### Description of the Bar Graph: 1. **Title and Labels**: - The title of the graph is Bar Graph: Results of a Survey. - The labels on the bars are a, b, c, and d. 2. **Bars and Colors**: - The horizontal bars are blue, orange, green, and purple. - The x-axis is labeled a and the y-axis is labeled b. 3. **Bars and Colors**: - The horizontal bars are blue, orange](<2412.02591v2/imageFile1.png>)



*Figure 1: An example of the column permutation in the exhaustive algorithm. (a) Initially, we append an identity matrix to the boundary matrix. (b) Assume we have processed the first k columns. The blue entries represent non-zero entries, and the dark blue boxes represent the pivots. To apply these pivots to next k columns (shown in gray), the first k columns must contain k pivots. (c-d) If there is a a zero column (shown in orange), we transpose it with the first non-zero entry in the appended identity matrix, ensuring the processed k columns all have pivots.*

![The image is a grid of 50 tiles arranged in a 5x5 grid. Each tile is a square with a different color and pattern. The tiles are arranged in a horizontal row, with each tile occupying a single square. The tiles are of different colors, including blue, green, brown, and purple. The tiles are arranged in a grid pattern, with each tile occupying a single square. The tiles are of different colors, including blue, green, brown, and purple. The tiles are arranged in a grid pattern, with each tile occupying a single square. The tiles are of different colors, including blue, green, brown, and purple. The tiles are arranged in a grid pattern, with each tile occupying a single square. The tiles are of different colors, including blue, green, brown, and purple. The tiles are arranged in a grid pattern, with each tile occupying a single square.](<2412.02591v2/imageFile2.png>)



*Figure 2: An example of the row permutations. (a) A submatrix consisting of 8 columns where the 4 columns on the left have been reduced, and we need to apply the pivots to the following 4 columns (in gray). (b-c) Each row which has a pivot gets permuted to the bottom of the matrix in the same order as the columns – resulting in a lower triangular matrix. (d) Applying the Schur complement zeros out the gray entries on the bottom right and we update the entries in the upper right (dark gray). (e) With the pivot rows zeroed out on the right, we reverse the permutation.*

![In this image, we can see a diagram. There are two circles, which are connected by a line. There are some numbers written on the circles.](<2412.02591v2/imageFile3.png>)



If we are not in the base case (a single column), we must apply the pivots in the columns indexed by B (left half of the submatrix) to the the columns indexed by C (right half of the submatrix). As described in Equation ( 2 ), we construct the permutation $P_{L}$ such that

$$
( P_{L} \cdot \mathbf R ) [ \cdot , B \cup C ] = \begin{bmatrix} \mathbf R [ \bar{L} , B ] & \mathbf R [ \bar{L} , C ] \\ \mathbf R [ L , B ] & \mathbf R [ L , C ] \end{bmatrix}
$$

By construction of $P_{L}$ , R [ L,B ] is lower-triangular with non-zero diagonal entries and so is full-rank. We can then apply the Schur complement to zero out all the corresponding rows in the columns $C , R [ L,C ] \gets 0$ and update the rest of the rows:

$$
\underbrace{R [ \bar{L} , C ]}_{C_{1}} \leftarrow \underbrace{R [ \bar{L} , C ] - \underbrace{R [ \bar{L} , B ]}_{B_{1}} \cdot \underbrace{R [ L , B ]^{- 1}}_{B_{2}^{- 1}}}_{B_{2}^{- 1}} \cdot \underbrace{R [ L , C ]}_{C_{2}} = R [ \bar{L} , C ] - R [ \bar{L} , B ] \cdot \Lambda
$$

To update matrix V , we perform the same operations $\Lambda = R [ L,B ] - 1 \cdot R [ L,C ]$ on V :

$$
V [ \bar{L} , C ] \leftarrow V [ \bar{L} , C ] - V [ \bar{L} , C ] \cdot \Lambda
$$

Once the left half of the matrix has been applied to the right half, we recurse on the second half, i.e., C . Here again, because R [ L,B ] is lower-triangular full-rank and $R [ L,C ] = 0$ , the first column of R [ · ,C ] is reduced. We now prove correctness.

Lemma 4.4. When the recursion base case is reached for column k , R [ low ( j ) ,k ] $= 0$ for all $j < k$ . Proof. For $k = 1$ , this is tautological since low ( j ) is empty. For $k > 1$ , we observe that entry R [ low ( i ) ,k ] was made zero when $i \in B$ and $k \in C$ . As the recursion always splits the column range in half, this condition must be satisfied somewhere in the recursion before the base case of k is reached.

Proof of Lemma 4.3. We observe that when $P_{L}$ is applied, the columns corresponding to L have a pivot. By construction, $P_{L}$ places the pivots onto the diagonal in the bottom $| L |$ rows. For any of these rows above the diagonal, the entries are 0 by Lemma 4.4 , implying the bottom k × k matrix is lower triangular.

Theorem 4.5. Algorithm 3 computes the persistence diagram correctly. Proof. It suffices to show that for all i , low ( i ) is computed correctly. For $i = 1$ , this is trivially true. For $i > 1$ , we first note that at any step of the algorithm, we maintain

$$
R = D \cdot V ,
$$

as any operations on R are applied to V . Assuming the pivots are correct for $j < i$ , by the above and Lemma 4.4 , V gives a transformation depending only on columns $j < i$ , such that all elements in R [ low ( j ) ,i ] $=$ 0. If low ( i ) does not exist, D [ · ,i ] was in the span of the previous columns and hence a cycle. If low ( i ) exists, it is distinct from { low ( j ) } j<i and so we have a new pairing. The correctness of this pairing follows from the Pairing Uniqueness Lemma [ 16 ].

Running Time Analysis The base case takes O ( 1 ) time. For a general step where $| B | = | C | = k$ , applying the update requires a row permutation of a 2 n $×$ 2 k matrix which takes O ( nk ) time. We must compute $R [ L,B ] - 1$ which is inverting a k $×$ k matrix. Multiplying it with R [ L,C ] takes $O ( k \omega )$ time as both are k $×$ k matrices. Finally, we multiply this product with the n $×$ k matrix R [ ¯ L,C ], which by Lemma 3.1 , takes $O ( nk \omega - 1 )$ time. Solving the recursion, we find that the total running time is $O ( n \omega )$ for $\omega > 2$ or O ( $n^{2}$ log n ) if $\omega =$ 2. For completeness, we include the derivation in Appendix B.

## 5 Row Algorithm

While Algorithm 3 produces the exhaustive reduction in matrix multiplication time, a natural question is whether the same can be done for computing the lazy reduction (and its representatives). In this section, we present an algorithm which achieves this. We first give the iterative version before describing how it can be computed in $O ( n \omega ).$ The idea behind this reduction is to consider rows from the bottom up. Observe that a

**Algorithm 4 Incremental Row Algorithm**

1: $R = D$ 2: $V = I n , U = I n$ 3: for $i = n$ to 1 do 4: if lft ( i ) is defined then 5: $j \gets$ lft ( i ) 6: for $j ' > j$ and low $( j ' ) = i$ do 7: $\alpha \gets R [ i,j ' ] / R [ i,j ]$ 8: $R [ \cdot ,j ' ] \gets R [ \cdot ,j ' ] - \alpha \cdot R [ \cdot ,j ]$ 9: $V [ \cdot ,j ' ] \gets V [ \cdot ,j ' ] - \alpha \cdot V [ \cdot ,j ]$ 10: $U [ j, \cdot ] \gets U [ j, \cdot ] + \alpha \cdot U [ j ' , \cdot ]$

(equivalently, $U [ j,j ' ] \gets \alpha$ )

pivot in the bottom row can be directly identified in D as the earliest (leftmost) non-zero entry, similarly as the pivot in the first column can be directly identified. To formalize the notion of the earliest eligible pivot in a given row, we define

$$
\text{lift} ( i ) = \arg \min_{j} \{\text{R} [ i , j ] \neq 0 \colon \text{low} ( j ) = i \}
$$

̸ As in the case of low , we only apply this function to R . The function lft returns the earliest column j which is non-zero and is the lowest such non-zero entry in its column. This condition is important as the earliest (left-most) non-zero entry may have pivots below it. This column is used to zero out the i -th row in columns to the right provided they do not already contain a pivot, i.e., if low $( j ) = i$ , then the column is applied. This condition is why we obtain equivalence with the lazy reduction we do not apply the pivot column to later columns which already contain a pivot.

Lemma 5.1. Algorithm 4 is produces the same decomposition as the lazy reduction. Proof. This is equivalent to [ 3 , Theorem 3.1 ].

### 5.1 Fast Row Algorithm

The fast version of Algorithm 4 is given in Algorithm 5. As above, it reduces the matrix row by row from the bottom up. This variant does not require matrix inversion and requires less padding. The trade-off is that it requires a more complex sequence of updates. Assuming D is an n × n matrix, we set $m < n$ such that $m + n = 2 x$ . We initialize our matrices as:

$$
R = \left[ \begin{array}{c|c} D & I_{m+n} \end{array} \right], \quad \Lambda = I_{m+n}.
$$

To simplify notation, from this point on, we assume n is a power of 2 (as $m + n < 2 n$ ), so the resulting matrix is D I n . This ensures that lft ( i ) is defined for all i in R , i.e., the identity matrix ensures there are no zero rows.

**Algorithm 5 Row Algorithm( A = [ i,j ])**

1: if $i = j$ then $▷$ leaf 2: $k \gets$ lft $( n - i + 1 ) ▷$ Identify the pivot in row $( n - i + 1 )$ 3: Update permutation matrix P with transposition $i ↔ k$ . 4: $( P \cdot \Lambda \cdot P )[ i,j ] \gets - ( R \cdot P )[ n - i +1 ,j ] ( R \cdot P )[ n - i +1 ,i ]$ for $j > i$ . 5: $( R \cdot P )[ n - i + 1 ,{i + 1 ,...,n} ] \gets 0 ▷$ Zero out $( n - i + 1 )$ -st row; see Remark 5.2 ; 6: else 7: $B \gets$ [ $i, ( i + j ) / 2$ ) $▷$ left child 8: $C \gets$ [( $i + j$ ) / 2 ,j ] $▷$ right child 9: $C_{r} \gets n - C + 1 ▷$ Rows are indexed top-down but the algorithm goes bottom up 10: Recurse on B 11: Apply updates from rows B to $C : ▷$ full row update; ( k $×$ k ) $·$ ( k $×$ n ) ( R $·$ P )[ $C_{r}$ , $·$ ] $←$ ( R $·$ P )[ $C_{r}$ , $·$ ] + ( R $·$ P )[ $C_{r}$ ,B ])( $P \cdot \Lambda \cdot P$ )[ B, $·$ ] 12: Recurse on C 13: $¯ A r = [ 1 ,i ] ▷$ everything above A 14: Full column update: $( R \cdot P )[ ¯ A r ,C ] \gets ( R \cdot P )[ ¯ A r ,C ] + ( R \cdot P )[ ¯ A r ,B ] \cdot ( P \cdot \Lambda \cdot P )[ B,C ] ▷ ( n \times k ) \cdot ( k \times k )$

It will be convenient to again consider permutations, but here they will be column permutations. We build up the permutation incrementally. In the i -th step, we update the permutation with the column transposition

$$
\pi ( i ) \colon \text{lift} ( n - i + 1 ) \leftrightarrow i .
$$

which we use a shorthand for the two mappings lft $( n - i + 1 ) \to i$ and $i \to$ lft $( n - i + 1 ).$ As lft is always defined, two columns are permuted in each step. The permutation after i steps is then given by

$$
P_{i} = \prod_{j \leq i} \pi ( j ) .
$$

Note that we apply the permutations in order, i.e., for $i = 1 , 2$ ,... While the columns corresponding to lft $( n - i + 1 )$ are fixed at i for all permutations, the original column at i may be permuted several times. In the algorithm, we omit the subscript; P refers to the accumulated permutation. As in the case of the column algorithm, we perform the permutations implicitly. We observe that in the permuted order, we arrange pivots on the left along the anti-diagonal, see Figure 4.

We keep track of the operations directly in the matrix $\Lambda$ which is initialized as the identity matrix. Before delving into the details of the algorithm, assume that the all rows below the i -th row have been reduced.



*Figure 4: The permutation of the columns so that the pivots in the bottom k rows are on the right. An important observation is that despite this reshuffling of columns, because of the definition of lft ( · ), we never “accidentally” reduce columns which occur earlier in the filtration order.*

The updates in the i -th row are given by

$$
( P \cdot \Lambda \cdot P ) \left [ i , j \right ] = \begin{cases} - \frac{( R \cdot P ) [ n - i + 1 , j ]}{( R \cdot P ) [ n - i + 1 , i ]} & j > i \\ 1 & n - i + 1 = j \\ 0 & \text{else} \end{cases}
$$

We digress here to explain why this works. As mentioned above, if we consider the column-permuted matrix R · P , it has the pivots in the bottom-left along the anti-diagonal (Figure 4 ). The i -th column corresponds to the column given by lft $( n - i +1 )$ (in the original filtration order). Hence, it is the earliest column with a non-zero entry in the i -th row from the bottom such that there is no pivot below. For any non-zero entry to the right of i , it must occur to the right of i in the original filtration order (before permuting), or it would have been returned by lft $( n - i + 1 )$ rather than the current i -th column in the permuted order.

Remark 5.2. Hence in Line 5 , all non-zeros in the row to the right of i are pivots (lowest entries), because every column with a pivot below $n - i + 1$ has been moved to the left of i in the permuted order and so no reduction will be applied, i.e., coefficient will be 0.

Finally, as we permute the columns we must also permute the rows of the $\Lambda$ matrix so that the recorded column operations match. This represents the base case of the recursion. As $\Lambda$ is initialized as the identity matrix, the case $n - i + 1 = j$ is taken care of implicitly. Returning to the algorithm, we proceed by recursing as in the column algorithm, but on the rows rather than on the columns. We divide the rows of the sub-matrix into the top and bottom halves. We recurse first on the bottom half. Because the pivots are determined by rows below the current one, we can directly identify the pivot with lft .

The base case is a single row. By assumption, rows below the current row have been updated. At row $n - i + 1$ , we first find the left-most non-zero entry and perform a column permutation setting the column lft $( n - i + 1 )$ to the i -th column, making $R [ n - i,i ] a$ pivot as required. By the reasoning above, we may zero out the row, recording the operations in $\Lambda$ and we can update $( R \cdot P )[ n - i,{i + 1 ,...,n} ] \gets$ 0.

Though the recursion proceeds as in Algorithm 3 , splitting the matrix into the indices in the “first half” and “second half,” a complication is that the columns are processed left to right (same as the indexing),

$$
C_{r} = n - C + 1 ,
$$

by which we mean for any $i \in C , i \to n - i +$ 1. This indexes the rows from the bottom of the matrix as required by the algorithm. Returning to the recursion, once we processed the columns for B , we first apply the updates to the rows $C_{r}$ ,

$$
( R \cdot P ) [ C_{r} , \cdot ] \leftarrow ( R \cdot P ) [ C_{r} , \cdot ] + ( R \cdot P ) [ C_{r} , B ] \cdot ( P \cdot \Lambda \cdot P ) [ B , \cdot ] ,
$$

shown in orange in Figure 5. Note that though B r is shown in Figure 5 , it is not used explicitly in the algorithm as all the operations in those rows have already been performed. On the other hand, B in $( P \cdot \Lambda \cdot P )[ B, \cdot ]$ and in ( R · P )[ $C_{r}$ ,B ] do not need to be reversed, as they both represent column operations which are in the standard order. After this, the first row in $C_{r}$ is now up-to-date with all operations from rows below it. This allows us to correctly identify lft for this row and perform the column transposition accordingly. Hence, when we recurse on C , the base case can be carried out.

Returning from C , we complete the recursion by performing the update operations on the remaining parts of the columns in C , shown in green in Figure 5. Note, the columns are indexed by C not $C_{r}$ . Let $¯ A r =$ { 1 ,..., min( $C_{r}$ ) $- 1$ } . The column update is given as

$$
( R \cdot P ) [ \bar{A}_{r} , C ] \leftarrow ( R \cdot P ) [ \bar{A}_{r} , C ] + ( R \cdot P ) [ \bar{A}_{r} , B ] \cdot ( P \cdot \Lambda \cdot P ) [ B , C ]
$$

Now all columns prior to and including C and all rows below and including $C_{r}$ are up to date. We show the update sequence in Figure 5 (omitting the permutations). Note that in the column update, the rows in ¯ A r are correctly indexed, so no reversal is necessary.

**Theorem 5.3. Algorithm 5 computes the lazy reduction.**

Proof. The algorithm is equivalent to Algorithm 4 , but it performs the operations by batches. Observe that in the base case for row $n - i + 1$ , the column operations recorded in $\Lambda$ are equal to $\alpha$ in the i -th step of Algorithm 4. If we performed the operations on the entire column, we would, in fact, obtain exactly Algorithm 4 (since there would be no need for any further updates in the recursion).

Hence, to show correctness, we need to ensure that we can correctly compute lft ( i ) in each base case. This is trivially true in the first step. In further steps, we need to ensure that all the column operations have been applied to ( R $·$ P )[ C, $·$ ]. We can directly verify that any column operations coming from row $j < i$ , fall in exactly one B (with i in C ) in the recursion and hence are applied before the base case for i . It only remains to show that the columns ( R $·$ P )[ $·$ ,C ] are up to date before the columns operations from rows in $C_{r}$ are applied in the appropriate rows of ( R $·$ P )[ ¯ A r , $·$ ], which is done in line 14.

$$
\text{Theorem 5.4. Matrix } U = 2 \cdot I - \Lambda \text{ satisfies } D = R \cdot U, \text{ or equivalently, } V = U^{-1}.
$$

Proof. During a regular reduction, Algorithm 4 , updates in matrix U use row operations to “undo” the column operations in matrices R and V . When a column j in R is subtracted from column j $′$ , which itself has not been used to reduce any other column, its corresponding row U [ j $′$ , $·$ ] contains a single diagonal element. – compare Algorithms 1 and 2. In this case, the update in matrix U is equivalent to setting entry U [ j $′$ ,j ] to the coefficient that is the negation of the coefficient used for the column update. When a column j in R is subtracted from column j $′$ , which itself has not been used to reduce any other column, its corresponding row U [ j $′$ , $·$ ] contains a single diagonal element.

Algorithm 5 satisfies this property (which is why it produces a lazy reduction): when an update is recorded in matrix $\Lambda$ , the column that is being updated has not yet been used to reduce any other column. Therefore, off the diagonal, the corresponding matrix U is the negation of matrix $\Lambda$ compare the coefficients in Algorithm 5 with $\alpha$ in Algorithm 4. On the diagonal, both $\Lambda$ and U are all ones. So adding 2 · I to $- \Lambda$ recovers U .

Remark 5.5. As U is an upper triangular matrix, by Theorem 4.2 , it can be inverted in $O ( n \omega )$ -time as required.



*Figure 5: The updating steps in Algorithm 5. On the right we have the recursion tree for the rows, as opposed to the columns which is the same as in Figure 3 as rows proceed bottom-up and columns proceed left to right. When we return from the recursion on B (or B r ), we have zeroed out all the entries to the right of columns B in rows B r . We then first need to apply these to the rows in C r (line 11 in Algorithm 5), shown in orange. We can then identify the correct pivot columns for rows C r (after recursing), and complete the step by applying the operations from columns B to C above. This is shown in green and corresponding to the right of A r = B r ∪ C r in the recursion tree (the rows above A r – denoted ¯ A r ) to the right of the dashed line. This makes columns B ∪ C and rows A r completely processed/reduced.*

Running Time Analysis The base case takes O ( n ) time as recording the permutation and zeroing out operations each take linear time. Hence the base case takes O ( n ) time. For the general case, we need to first apply the column permutations. Since we are permuting at most an n $×$ k (or k $×$ n ) matrix, applying the permutation (and undoing it) takes O ( nk ) time by Lemma 3.2. Next, applying the row updates from B r to $C_{r}$ , assuming $| B | = | C | = k$ , requires multiplying $( P \cdot \Lambda \cdot P )[ B, \cdot ], a k \times n$ matrix, with ( R $·$ P )[ $C_{r}$ ,B ], a k $×$ k matrix taking $O ( nk \omega - 1 )$ time by Lemma 3.1. To complete the update, the subtractions take O ( nk ) time. To compute the column updates, we must multiply ( R $·$ P )[ ¯ A r ,B ], an n $×$ k matrix with $( P \cdot \Lambda \cdot P )[ B,C ], a k \times k$ matrix, which again takes $O ( nk \omega - 1 )$ time. Putting these together, we find that the full step can be completed in $O ( nk \omega - 1 )$ time and as in the column algorithm case, solving the recursion gives an $O ( n \omega )$ running time for $\omega > 2$ and a O ( $n^{2}$ log n ) running time for $\omega =$ 2.

## 6 Discussion

In this paper, we have shown that in the case of persistent (co)homology, the standard bases returned by the exhaustive and lazy reductions can be computed in matrix multiplication time. It is worth noting that while both the columns and row algorithms utilize batch updates, they are qualitatively different.



1. Only the column algorithm critically relies on fast matrix inversion.

2. The row algorithm update sequence is substantially more complex but only requires column permutations.

While one can use the column algorithm to compute the lazy basis and the row algorithm for the exhaustive basis in the same asymptotic running time, the resulting algorithms are both complex and not particularly illuminating. There are also numerous technical complications to deal with, so we omit them from this paper. Another outstanding issue is whether it is possible to transform the exhaustive representatives to lazy representatives and vice versa without redoing the reduction. Again we believe this possible, but omit it due to space and because the asymptotic running time is same. Finally an open question is regarding representatives in zigzag persistence which has recently been addressed in [ 25 , 26 ].

## Acknowledgement

Dmitriy Morozov was supported by the Director, Office of Science, Office of Advanced Scientific Computing Research, of the U.S. Department of Energy under Contract No. DE-AC02-05CH11231. Primoz Skraba was supported by the EPSRC AI Hub on Mathematical Foundations of Intelligence: An “Erlangen Programme” for AI No. EP/Y028872/1.

## A Matrix Inversion in Matrix Multiplication Time

We present a simplified proof for the special case of triangular matrices. This can be shown with greater generality and was stated as Fact 4 in Strassen’s original paper on sub-cubic matrix multiplication time [ 27 ]. While Strassen’s result required certain submatrices to be non-singular, these constraints where removed by Bunch and Hopcroft [ 28 ]. We include the proof for completeness. Importantly, this makes no assumption on the underlying field. We provide the proof for lower triangular matrices, but it holds equivalently for upper triangular, by transposition. We begin with the following observation.

Observation A.1. A lower triangular matrix is non-singular if and only if the diagonal elements are nonzero. This follows from the fact that the columns must be linearly independent and so the matrix is full rank and hence invertible. Theorem 4.2. Inverting a (square) non-singular (upper or lower) triangular matrix can be done in matrix multiplication time. Proof. Consider a non-singular lower triangular n × n matrix A . Further assume that n is a power of 2. If it is not we can consider the matrix A 0

$$
\begin{bmatrix} A & 0_{n \times m} \\ 0_{m \times n} & I_{m} \end{bmatrix}
$$

where $m = 2 \lceil log 2 n \rceil - n$ . From now on assuming n is a power of 2 , we can write A as

$$
\begin{bmatrix} B & 0 \\ C & D \end{bmatrix}
$$

where all sub-matrices are of size $n^{2}$ . Observe that B and D lower diagonal with a non-zero diagonal elements – the same as A – and so are non-singular. Through direct computation, we note that the inverse is

$$
A^{- 1} = \begin{bmatrix} B^{- 1} & 0 \\ - D^{- 1} C B^{- 1} & D^{- 1} \end{bmatrix}
$$

That this is the inverse can be directly verified by expanding $A - 1 \cdot A$ or vice versa. Therefore, we can compute the inverse by computing the inverse of B and D . Therefore, we require the inversion of two $n^{2}$ $×$ $n^{2}$ matrices, and two matrix multiplications. As B and D are both lower triangular with a non-zero diagonal, we can further subdivide and compute the inverse as above but with the initial matrix being of size $n^{2}$ $×$ $n^{2}$ . To bound the total computational complexity, we let I ( n ) be the time to invert an n n (lower triangular)

$×$ matrix and M ( n ) the time to perform matrix multiplication of two n $×$ n matrices. This gives the recurrence

$$
I ( n ) \leq 2 I \left ( \frac{n}{2} \right ) + 2 M \left ( \frac{n}{2} \right ) .
$$

Let $M ( n ) \leq c_{1} n \omega$ with $M ( 1 ) = c_{1}$ and $I ( 1 ) = c_{2}$ . Assuming $n = 2 k$ , we can write

$$
\begin{aligned}
M ( 1 ) & = c_{1} \text{and} I ( 1 ) = c_{2} . \text{Assuming} n = 2^{k} , \text{we can write} \\ I ( 2^{k} ) & \leq 2 I ( 2^{k - 1} ) + 2 M ( 2^{k - 1} ) \\ & \leq 2^{2} ( I ( 2^{k - 2} ) + M ( 2^{k - 2} ) + 2 M ( 2^{k - 1} ) ) \\ & \vdots \\ & \leq 2^{k} I ( 1 ) + \sum_{j = 1}^{k} 2^{j} M ( 2^{k - j} ) \\ & \leq c_{2} 2^{k} + c_{1} \sum_{j = 1}^{k - 1} 2^{j} 2^{( k - j ) \omega} \\ & \geq 2 ,
\end{aligned}
$$

We observe that since ω ≥ 2, n is a power of 2, for ℓ = 2 k ,

$$
\sum_{j = 1}^{k - 1} 2^{- j ( \omega - 1 )} \leq \sum_{j = 0}^{\infty} 2^{- j ( \omega - 1 )} \leq 2
$$

Substituting in and using that $n = 2 k$ , we get

$$
\begin{aligned}
I ( 2^{k} ) & \leq c_{2} 2^{k} + c_{1} 2^{k \omega} \sum_{j = 1}^{k - 1} 2^{- j ( \omega - 1 )} \\ & \leq c_{2} 2^{k} + 2 c_{1} ( 2^{k} )^{\omega} = O ( n + n^{\omega} ) = O ( n^{\omega} ) .
\end{aligned}
$$

## B Analysis of Persistence Recursion

For all the algorithms, the recursion is of the form

$$
P H ( \ell ) \leq 2 P H \left ( \frac{\ell}{2} \right ) + c_{1} n \left ( \frac{\ell}{2} \right )^{\omega - 1}
$$

where PH ( n ) is the running time for persistent (co)homology on a filtration with n cells and $c_{1}$ takes into account the constant in terms of number of applications as well as for the matrix multiplication. Assuming

$$
of 2 , \, \text{for} \ell = 2^{k} , \\ \quad P H ( 2^{k} ) \leq 2 P H ( 2^{k - 1} ) + c_{1} n 2^{( k - 1 ) ( \omega - 1 )} \\ \leq 2^{2} P H ( 2^{k - 2} ) + 2 c_{1} n 2^{( k - 2 ) ( \omega - 1 )} + c_{1} n 2^{( k - 1 ) ( \omega - 1 )} \\ \vdots \\ \leq 2^{k} P H ( 1 ) + c_{1} n \sum_{j = 0}^{k - 1} 2^{j} 2^{( k - 1 - j ) ( \omega - 1 )} \\ = 2^{k} P H ( 1 ) + c_{1} n 2^{( k - 1 ) ( \omega - 1 )} \sum_{j = 0}^{k - 1} 2^{j - j ( \omega - 1 )} \\ = 2^{k} P H ( 1 ) + c_{1} n 2^{( k - 1 ) ( \omega - 1 )} \sum_{j = 0}^{k - 1} 2^{- j ( \omega - 2 )} \\ > 2 , \, \text{we note that}
$$

Assuming $\omega > 2$ , we note that

$$
\sum_{j = 0}^{k - 1} 2^{- j ( \omega - 2 )} \leq \sum_{j = 0}^{\infty} 2^{- j ( \omega - 2 )} = \frac{1}{1 - 2^{- ( \omega - 2 )}} \leq c_{2}
$$

Observe that $c_{2}$ depends on $\omega$ . Substituting in, we get

$$
\begin{aligned}
P H ( 2^{k} ) & \leq 2^{k} P H ( 1 ) + c_{1} n 2^{( k - 1 ) ( \omega - 1 )} \sum_{j = 0}^{k - 1} 2^{- j ( \omega - 2 )} \\ & \leq 2^{k} P H ( 1 ) + c_{1} c_{2} n 2^{( k - 1 ) ( \omega - 1 )}
\end{aligned}
$$

If $ℓ = n = 2 k$ , then

Since PH $( 1 ) \leq c 3 n$ , we get as required. If $\omega = 2$ , then

$$
P H ( n ) \leq n P H ( 1 ) + \frac{c_{1} c_{2}}{2} n^{\omega}
$$

$$
P H ( n ) \leq O ( n^{2} + n^{\omega} ) = O ( n^{\omega} )
$$

$$
\sum_{j = 0}^{k - 1} 2^{- j ( \omega - 2 )} = \sum_{j = 0}^{k - 1} 1 = k \leq c_{4} \log n
$$

Substituting in, we get

$$
\begin{aligned}
P H ( 2^{k} ) & \leq 2^{k} P H ( 1 ) + c_{1} n 2^{( k - 1 )} \sum_{j = 0}^{k - 1} 2^{- j ( \omega - 2 )} \\ & \leq 2^{k} P H ( 1 ) + c_{1} c_{2} c_{4} n 2^{( k - 1 )} \log n \\ & = 2^{k} P H ( 1 ) + \frac{c_{1} c_{2} c_{4}}{2} n 2^{k} \log n \\ & \leq O ( n^{2} + n^{2} \log n ) = O ( n^{2} \log n )
\end{aligned}
$$

![The image is a bar chart with two sets of bars. Each set of bars is colored in a different color, with the colors being blue and gray. The x-axis is labeled a and the y-axis is labeled b. The bars are arranged in a horizontal line, with the blue bars on the left and the gray bars on the right. The bars are of different lengths, with the blue bars being shorter than the gray bars. The bars are not aligned in any particular order. ### Description of the Bar Chart: 1. **X-Axis (a)**: The x-axis is labeled a and has a label b. 2. **Y-Axis (b)**: The y-axis is labeled b and has a label a. 3. **Bars**: There are two sets of bars, each set of bars colored in blue and gray. The blue bars are on the left and the gray bars](<2412.02591v2/imageFile4.png>)

![In this image there is a diagram, which is a table, and there are some numbers on the table.](<2412.02591v2/imageFile5.png>)